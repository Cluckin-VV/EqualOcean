# 亿欧出海运营 Agent：一期最小 POC 说明

状态：内部预研 / 本地模拟数据验证

更新时间：2026-08-06（Asia/Shanghai）

## 1. 本期只做什么

一期不先搭建完整的运营 Agent、多 Agent、邮件、客户画像和线索系统。

一期只验证一件事：

```text
编写一组假的用户行为数据
→ 读取用户行为
→ 分析行为强度和行为主题
→ 作出当前行为判断
→ 输出判断依据
```

本期的目标不是证明真实客户会购买，而是证明最小模块能够读懂结构化行为，并给出可解释、可复现的判断。

## 1.1 本文采用的 Agent 工程标准

`agent.docx` 提供的是工程方法，不是要求一期把所有能力都实现。当前 POC 只吸收以下原则：

- 先判断是否需要 Agent。当前行为分析中的计数、赋分和阶段判断步骤固定，因此优先使用 Python 规则，不调用 LLM。
- Agent 的完整形态是“LLM + Harness”。本期的 Harness 先用最小程序实现：读取输入、保存状态、执行规则、绑定证据、校验输出和停止。
- 每次运行都保留输入快照、行为 ID、规则版本、输出结果和运行日志，保证可以重放和复盘。
- 只读取当前行为分析需要的用户资料和行为，不把无关历史或全量内容放入上下文。
- 先定义行为类型、主题、对象、时间和证据字段，再进行判断，不让模型自行发明业务含义。

以下能力理解为后续扩展，不属于一期验收：ReAct 多轮工具循环、Plan-then-Execute 长任务、Loop Engineering、Prompt Caching、企业本体系统、通用实时检索和多 Agent 协同。当前仅增加了一个受限的亿欧数据官网只读适配器，用于初步咨询，不改变一期行为判断范围。

## 2. 目标客户和行为数据

Nasdaq 是项目预留的目标客户位，使用其正常公司名称和投资方/资本市场场景。`partner_demo_nasdaq` 是系统内部 ID，不改变客户名称。

当前 8 条行为是为了验证最小架构而准备的 POC 行为样例；正式确认客户行为数据后，直接替换行为输入即可，不需要重写行为分析规则。

同时预留一个主要服务客户位：

```text
partner_primary_cheng_shaokai / 程少楷 / reserved_empty
```

当前不填充程少楷的行为、兴趣或投资偏好，后续最终确认版再启用。

客户资料位于：

```text
D:\AAAAA\agent_poc\data\partners.json
```

模拟行为数据位于：

```text
D:\AAAAA\agent_poc\data\simulated_behavior.jsonl
```

## 3. 模拟行为内容

当前为 Nasdaq 构造了 8 条围绕“中国 AI 硬件投资”的行为：

1. 首次浏览相关情报；
2. 搜索“中国 AI 硬件融资与投资机会”；
3. 查询安克创新这一潜在标的公司；
4. 连续查看相关情报；
5. 下载投资研究报告；
6. 收藏潜在投资标的信息；
7. 订阅 AI 硬件投资主题；
8. 索要投资标的资料。

这些行为用来模拟一个投资研究团队从“初步关注”逐渐进入“认真评估”的过程。

## 4. 最小运行逻辑

### 第一步：读取数据

程序读取两个输入：

- 客户资料：用户名称、角色、标签和客户位状态；
- POC 行为数据：行为类型、发生时间、主题、对象和样例标记。

本地还会读取 `intelligence_export_100.xlsx`，只是为了保留新闻来源引用和测试数据质量，不在本期实现新闻理解或复杂推荐。

### 第一步半：检查数据 Agent 交接状态

运营 Agent 不直接相信原始新闻快照，而是优先读取：

```text
D:\AAAAA\agent_poc\data\data_agent_handoff.json
```

只有同时满足以下条件的内容，才允许进入运营 Agent 的来源证据：

```text
company_match_status = matched
quality_status = passed
translation_status = passed
dedup_status = unique 或 merged
publish_status = candidate
source_url 有效
data_version 和 content_version 存在
```

如果内容属于原文更新但译文尚未完成、完全重复、公司匹配不明确或质量检查未通过，运营 Agent 必须阻断该内容，并输出 `data_handoff.feedback_items`，要求数据 Agent 重新处理，不能自行猜测或继续推荐旧版本。

交接记录的最小字段为：

```text
content_id, source_url, company_ids, company_match_status,
change_type, quality_status, translation_status, dedup_status,
publish_status, data_version, content_version
```

其中 `change_type` 用于区分新内容、事件更新和完全重复；`quality_flags` 可携带数据 Agent 的校验告警；所有版本字段都必须原样保留，便于追溯和复盘。

### 第一步半之二：读取亿欧数据官网

运营 Agent 已增加 `agent_poc/iyiou_data_agent.py` 作为官网只读适配器，目标地址为：

```text
https://data.iyiou.com
https://apidata.iyiou.com
```

咨询入口会先识别问题类型，再调用对应官方数据入口，当前已配置资讯、融资/投资事件、企业、政策、招投标、行业指标入口和首页汇总。返回结果统一包含记录类型、记录 ID、标题、时间、摘要、关键属性、来源和官网地址；具体入口是否可读取取决于官网权限。

当前适配器只执行 GET 读取，不执行订阅、关注、导出、提交、发送或修改操作。官网全局搜索、部分研究报告和部分招投标接口会要求登录；如果没有授权，系统返回 `needs_auth`，不使用 POC 行为或本地新闻冒充官网搜索结果。运行示例：

```powershell
$py = "C:\Users\CluckinVV\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe"
& $py agent_poc/run_consult.py "今天有哪些融资事件" --limit 5
```

如果后续确认拥有对应官网权限，可通过环境变量 `IYIOU_DATA_AUTH` 提供授权后的 `Auth` 值；不得把令牌写入代码、文件、日志或回复。

### 第二步：统计行为

程序按照行为类型为每条行为赋分，并统计最近最多 10 条行为。

当前行为权重如下：

| 行为 | 分值 | 说明 |
|---|---:|---|
| 浏览文章 `article_view` | 1 | 低强度关注 |
| 重复查看 `repeat_view` | 2 | 重复关注 |
| 搜索主题 `search` | 2 | 主动探索 |
| 收藏 `favorite` | 2 | 保存信息 |
| 查询公司 `company_query` | 3 | 研究具体对象 |
| 下载报告 `report_download` | 4 | 深度获取内容 |
| 订阅主题 `subscription_request` | 4 | 主动持续关注 |
| 索要资料 `material_request` | 5 | 明确资料需求 |
| 询问合作 `cooperation_request` | 6 | 明确业务动作 |
| 请求会议 `meeting_request` | 6 | 明确沟通动作 |

计算方式是：

```text
行为分 = 最近最多 10 条有效行为的分值总和
```

### 第三步：作出行为判断

判断只使用规则，不调用大模型：

- 出现合作或会议请求：判断为“推进”；
- 出现下载、订阅或索要资料：判断为“评估”；
- 出现至少两次主动互动或重复关注同一主题：判断为“了解”；
- 只有浏览或低强度行为：判断为“关注”。

判断优先级为：

```text
推进 > 评估 > 了解 > 关注
```

### 第四步：输出证据

每个判断必须返回对应的行为 ID，例如：

- 哪一次搜索支持“主动探索”；
- 哪一次下载支持“深度获取内容”；
- 哪一次资料请求支持“评估阶段”。

没有证据时，不能把判断写成事实。

## 5. 当前 Nasdaq 样例结果

8 条行为对应的分数为：

```text
1 + 2 + 3 + 1 + 4 + 2 + 4 + 5 = 22 分
```

系统当前判断为：

- 用户：Nasdaq；
- 关注主题：中国 AI 硬件投资；
- 行为分：22；
- 当前行为阶段：评估；
- 规则判断置信度：0.90；
- 判断依据：下载报告、订阅主题、索要资料；
- 数据状态：当前行为为 POC 样例，来源咨询可读取亿欧数据官网；
- 是否需要人工审核：是。

这里的“评估”只表示当前输入行为已经从浏览和搜索发展到下载、订阅和索要资料。正式客户行为接入后，仍需依据真实证据和人工审核解释，不自动扩大为购买或投资结论。

## 6. 本期明确不做

本期暂时不做以下内容：

- 不搭建完整多 Agent 架构；
- 不实现邮件 Agent；
- 不实现自动线索跟进；
- 不建立正式客户画像；
- 不自动发送邮件或企业微信；
- 不自动发布网站内容；
- 不把当前 8 条 POC 行为直接当作正式客户行为记录；
- 不接入生产账号、生产数据库或付费接口；
- 不在缺少本机环境变量或真实官网来源时调用千问；已允许 Qwen Flash 对核验后的官网记录生成受限分析；
- 不把模拟数据当作真实客户行为。

客户画像、线索评分、订阅运营和邮件草稿属于后续扩展，不是一期验收内容。

## 7. 一期验收标准

满足以下条件即可认为一期最小 POC 跑通：

1. 能读取模拟用户和行为数据；
2. 能识别行为类型、主题和时间；
3. 能计算行为分；
4. 能输出关注、了解、评估、推进中的一个判断；
5. 判断结果包含行为 ID 作为依据；
6. 能校验数据 Agent 交接状态，并阻断不可用内容；
7. 合格来源保留 `data_version` 和 `content_version`；
8. 能区分模拟数据和真实数据；
9. 能重复运行并得到一致结果；
10. 不执行自动发送、发布或正式数据写入。

## 8. 如何验证

在 `D:\AAAAA` 下执行：

```powershell
$py = "C:\Users\CluckinVV\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe"
& $py -m unittest discover -s agent_poc/tests -v
& $py agent_poc/run_demo.py
```

当前验证结果：

- 单元测试：22/22 通过；
- 模拟用户：Nasdaq；
- 模拟行为：8 条；
- 行为分：22；
- 行为判断：评估；
- 规则置信度：0.90；
- 新闻来源引用：4 条；
- 数据 Agent 交接状态：4 条合格，0 条阻断；
- 千问调用：无。

## 10. 会后业务目标和 Agent 治理方案（2026-08-06）

### 10.1 服务对象和核心价值

服务对象调整为：计划在美股上市的海外企业。运营 Agent 不只是提供新闻列表，而是帮助客户识别跨市场信息差：

```text
亿欧数据事实
→ 判断中国市场与海外市场的关注权重差异
→ 解释对企业业务、融资、合规和上市准备的潜在影响
→ 给出来源、证据、不确定性和待确认问题
```

同一条国内新闻不能直接等同于海外市场影响。Agent 必须先保留事实和来源，再由规则或 Qwen 生成“影响候选”，不能把候选影响写成确定结论。

### 10.2 会话轮询和历史压缩

每次用户发来新问题时，不加载客户全部历史，而是按以下顺序处理：

1. 读取客户的稳定客户事实、当前目标、上市阶段、行业/国家、已确认约束和未解决问题；
2. 读取最近窗口，当前默认最多 6 条消息；
3. 从历史中只保留高价值消息：目标、约束、关键决定、用户纠错、未解决问题、来源证据、工具结果；
4. 删除重复内容、寒暄、无业务信息的闲聊和已被新决定覆盖的中间草稿；
5. 将压缩后的工作记忆和本次问题一起交给 Agent/Qwen。

当前实现位于 `agent_poc/conversation_memory.py`。它输出 `active_goals`、`constraints`、`decisions`、`corrections`、`open_questions`、`evidence_refs`、`recent_messages` 和被丢弃数量。

这里的“丢弃”分两层：

- 上下文丢弃：不再放入本轮模型上下文，立即减少 Token 和内存占用；
- 文件删除：必须遵守日志保留策略，不能因为上下文不用就立刻物理删除审计或合规记录。

### 10.3 会话日志和 15 天清理

不把所有信息放在同一个日志文件里，分为三层：

| 层级 | 内容 | 默认策略 |
|---|---|---|
| 原始会话日志 | 用户原话、工具调用输入输出、临时草稿 | 默认保留 15 天，先 dry-run 再清理 |
| 工作记忆 | 客户目标、约束、决定、纠错、未解决问题和证据索引 | 90 天或直到新版本替换，具体由授权和合同决定 |
| 审计/来源记录 | 运行 ID、规则版本、来源版本、权限和人工审核结果 | 不按 15 天原始会话策略删除；按合规要求或 legal hold 保留 |

清理规则：

- 有 `legal_hold=true`、`retention_class=audit` 或 `source_snapshot` 的记录不删除；
- 缺失时间戳或 JSON 损坏的记录先保留并报警；
- 清理命令默认只生成计划；只有显式 `--apply` 才重写文件；
- 删除前记录文件、截止时间、删除 ID 和保留 ID；
- 15 天是当前 POC 默认值，不是最终法律/合同结论。

实现和命令：

```powershell
$py = "C:\Users\CluckinVV\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe"
& $py agent_poc/run_cleanup.py --days 15
```

### 10.4 本地数据库方案

在没有正式数据库权限时，先用本地 SQLite 做 staging，不直接写生产库。当前表包括：

- `customers`：客户位、角色和状态；
- `source_records`：资讯、投资事件、企业、政策、招投标等统一来源记录；
- `conversation_messages`：会话消息索引；
- `conversation_memory`：压缩后的客户工作记忆。

来源记录按 `(record_type, record_id)` 幂等更新，保留标题、摘要、时间、来源、链接、关键属性和原始字段。当前已从亿欧数据官网写入：资讯 20 条、投资事件 20 条、企业 20 条、政策 20 条、招投标 5 条。

实现位于 `agent_poc/data_store.py`，同步命令为：

```powershell
$py = "C:\Users\CluckinVV\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe"
& $py agent_poc/run_ingest.py --category news --category investment --category company --category policy --category bidding --limit 20
```

### 10.5 Qwen 的使用边界

Qwen 只负责需要语义判断的部分：

- 中国与海外市场的关注权重差异候选；
- 对拟美股上市客户的潜在影响候选；
- 用户咨询意图和需要补充的资料；
- 在已核验来源基础上的结构化解释草稿。

Qwen 不负责：

- 自己搜索未经授权的数据；
- 修改数据库事实；
- 把推测写成上市、融资或合规结论；
- 自动发送客户消息或执行不可逆操作。

适配器位于 `agent_poc/qwen_client.py`，按照 DashScope OpenAI-compatible `/chat/completions` 入口实现。Key 只从 `DASHSCOPE_API_KEY` 读取，不写入文件、日志或输出；当前对话中已经暴露的 Key 不使用，必须先轮换。

### 10.6 官网实时咨询闭环

`agent_poc/run_live_agent.py` 已将一期需要的能力收口为一条命令：先读取 Nasdaq 模拟行为并生成行为阶段，再从亿欧数据官网读取最新融资记录，为缺少 `sourceLink` 的投资事件补齐对应企业详情页，然后将记录 ID、日期、摘要、融资属性和官网链接一起交给 Qwen Flash。

默认问题调整为：

```text
最新融资事件对拟在美股上市的海外企业有什么帮助
```

输出包括最新真实资讯、独立来源清单、官网详情链接、帮助分析、依据和不确定性。Qwen 只能使用本轮提供的 `source_records`，不得补造其他公司、数字、案例、监管事实或链接；即使模型正文遗漏链接，程序仍会独立输出官网来源清单。

### 10.7 供给侧竞争原则

运营 Agent 的差异化不定义为“更快地整理新闻”，而定义为一层可信的供给侧判断能力：持续吸收官网产生的新事实，帮助客户发现其自身难以完整看到的融资、行业、企业和政策变化，并把这些事实转成对业务判断、融资准备、合规准备、投资者沟通和上市节奏的帮助候选。

一期只验证“真实来源 + 可追溯链接 + 受限分析 + 人工审核”这一点，不扩展复杂多 Agent、自动销售或不可逆操作。后续只有在客户持续使用并产生纠错、反馈和决策结果后，才逐步沉淀动态 know-how；不能把公开 SOP、提示词或通用模型能力误写成护城河。

## 9. 必须说明的边界

第一，当前评分是行为强度分，不是购买概率、客户价值、会员等级或投资成功概率。

第二，当前判断是规则判断，不是模型判断。这样做是为了先证明流程简单、可解释、可重复。

第三，只有行为分析链路稳定以后，才考虑加入客户画像、线索评分、邮件草稿和其他 Agent。

第四，下一步应该增加不同类型的行为输入，覆盖只有浏览、重复搜索、下载资料、请求会议和无行为等情况，再观察规则是否合理。
