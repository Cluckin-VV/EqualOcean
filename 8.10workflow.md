# 亿欧出海运营 Agent Workflow（8.10 版本）

版本：8.10

生成日期：2026-08-10（Asia/Shanghai）

上一版本：`operation_agent_workflow8.06.md`
状态：一期 POC 最新执行基线

## 0. 版本管理规则

从本版本开始不再覆盖上一份 Workflow：

```text
operation_agent_workflow8.06.md  保留 8.06 状态
8.10workflow.md                  记录 8.10 状态
未来版本，例如：8.17workflow.md  基于 8.10 新建
```

每个新版本必须包含：版本日期、上一版本、已完成变化、尚未完成、运行命令、测试结果和边界。旧文件只用于前后对照，不再回写成最新状态。

## 1. 项目本源

本项目是亿欧智库面向计划、筹备或评估在美国资本市场上市的海外企业的出海运营 Agent。

它要帮助企业利用亿欧智库的数据供给获得信息差：同一条国内资讯在国内可能权重不高，但可能影响海外企业的融资准备、业务布局、合规判断、投资者沟通和上市节奏。

完整产品长期包括但不限于：

- 客户画像；
- 客户行为分析；
- 客户意向判断；
- 线索评分；
- 真实资讯咨询；
- 内容匹配与推荐；
- 邮件或沟通草稿；
- 订阅运营；
- 数据 Agent 交接；
- 会话 Memory；
- 日志、数据库和审计。

8.10 仍是最小 POC，不把规划中的功能伪装成已经上线。

## 2. 8.06 → 8.10 的实质变化

| 能力 | 8.06 状态 | 8.10 状态 |
|---|---|---|
| 客户行为 | Nasdaq 8 条模拟行为 | 保留 Nasdaq；程少楷作为真实命令行提问入口 |
| 行为判断 | 计分、阶段、证据 ID | 保持可运行，仍是核心模块 |
| 客户画像 | 不建立正式画像 | 可生成兴趣画像候选，不自动写入正式画像 |
| 数据来源 | 官网只读适配器初步接入 | 可按问题分类读取融资、新闻、政策、企业、行业和招投标入口 |
| Qwen | 独立 smoke 调用 | 任意问题必须先读取官网证据，再调用 Qwen Flash |
| 默认问题 | 融资事件有什么帮助 | 不再限制固定问题，默认问题仅作为示例 |
| 来源链接 | 部分结果只有结构化记录 | 独立输出来源 ID、日期、摘要和可打开官网链接 |
| 数据交接 | 模拟契约 | 支持质量、翻译、去重、公司匹配、发布状态和版本校验 |
| Memory | 设计 | 已有压缩算法和测试，尚未全面接入每轮咨询 |
| 日志清理 | 设计 | 已有 15 天 dry-run/清理工具，尚未定时运行 |
| 数据库 | 设计 | 已有本地 SQLite 幂等入库基础，尚未接正式数据库 |
| 测试 | 早期行为测试 | 当前完整测试不少于 23 项，以实际测试输出为准 |

## 3. 当前客户定义

### 3.1 Nasdaq

Nasdaq 是项目目标客户位。当前 8 条行为仍是 POC 模拟输入，用于验证行为分析，不代表从 Nasdaq 真实系统取得的用户行为。

### 3.2 程少楷

`partner_primary_cheng_shaokai` 是真实客户提问入口。命令行中由程少楷亲自输入的问题视为真实咨询，不再标记成模拟问题。

重要边界：一条真实提问只能证明“本轮问过什么”，不能自动推出长期兴趣、采购意向或正式画像。长期画像仍需多轮证据和人工确认。

## 4. 8.10 最小运行闭环

```text
客户输入任意问题
→ 保留本轮真实问题
→ 识别问题类型
→ 调用 data.iyiou.com 对应只读入口
→ 归一化来源 ID、标题、日期、摘要、属性和官网链接
→ 没有可信来源则阻断模型自由补写
→ 有来源则交给 Qwen Flash
→ 输出对客户的帮助、依据和不确定性
→ 人工审核
```

Nasdaq 行为 Demo 仍保留独立闭环：

```text
8 条模拟行为
→ 最近行为统计
→ 行为强度分
→ 关注/了解/评估/推进
→ 证据 ID
→ 画像候选与运营建议
```

## 5. 任意问题路由

| 问题关键词 | `query_type` | 官网入口方向 |
|---|---|---|
| 融资、投资、投资方、投资机构 | `investment` | 融资/投资事件 |
| 新闻、资讯、情报、动态、消息 | `news` | 资讯情报 |
| 政策、法规、行动方案、通知 | `policy` | 政策数据 |
| 公司、企业、工商、企业分析 | `company` | 企业数据 |
| 行业、指标、研究报告、图表 | `industry` | 行业研究/指标 |
| 招标、中标、采购公告 | `bidding` | 招投标数据 |
| 无法归类 | `search` | 官网综合搜索；可能需要权限 |

`query_applied=true` 表示官网接口实际使用了问题关键词；`false` 表示当前公开入口返回该类别的最新列表，Qwen 只能在这批记录范围内回答，不能声称完成了精确全站搜索。

## 6. 任意问题 PowerShell 入口

问题不是固定搭配。双引号内可替换为任何企业客户问题：

```powershell
Set-Location -LiteralPath "D:\AAAAA"
```

```powershell
$py = "C:\Users\CluckinVV\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe"
```

```powershell
& $py agent_poc/run_qwen_smoke.py "最近的跨境数据政策对准备赴美上市的企业有什么帮助？" --limit 5
```

再换一个问题也可以：

```powershell
& $py agent_poc/run_qwen_smoke.py "我完全不懂融资，请用大白话解释今天的融资事件对我们有什么用" --limit 5
```

问题模板见 `customer_question_templates_8.10.md`，当前提供 60 条。

## 7. 模型约束

Qwen Flash 只负责语义解释，不负责事实生成。它必须：

- 只使用本轮 `source_records`；
- 引用对应 `record_id`；
- 原样保留 `source_url`；
- 区分事实、推断和不确定性；
- 资料不足时明确说明不足；
- 把结论写成待人工审核的运营辅助。

Qwen 不得：

- 补造公司、融资金额、投资机构、政策、监管事实或案例；
- 把最新类别列表说成精确搜索结果；
- 自动更新正式客户画像；
- 自动发送邮件、企业微信或订阅消息；
- 输出确定的投资、法律、审计或上市结论。

## 8. 当前功能完成度

### 已可运行

- Nasdaq 模拟行为读取；
- 行为计分、意向阶段和证据 ID；
- 兴趣画像候选；
- 运营动作建议；
- 数据 Agent 交接校验与不合格内容阻断；
- 亿欧官网只读分类咨询；
- 官网链接补齐；
- Qwen Flash 受限分析；
- 独立来源清单；
- 运行日志、规则版本和人工审核标记；
- SQLite 来源记录幂等入库；
- 会话压缩和 15 天日志清理基础模块。

### 部分完成

- 客户画像：只有快照和候选，没有完整企业画像工作台；
- 线索评分：只有行为强度分，不是正式成交概率；
- 内容推荐：只有建议动作，没有完整排序和反馈学习；
- Memory：算法可测，未全面嵌入咨询入口；
- 日志清理：工具可运行，未配置自动定时任务；
- 数据库：本地 SQLite 可用，未接正式生产库。

### 尚未实现

- 邮件草稿 Agent；
- 自动邮件或企业微信发送；
- 正式订阅创建、推送和退订；
- CRM 自动任务；
- 正式客户线索评分模型；
- 真实 Nasdaq 行为接口；
- 完整人工审核后台；
- 多 Agent 自动协同。

## 9. 数据 Agent 交接规则

运营 Agent 接收内容前必须检查：

- `content_id`；
- `source_url`；
- `company_ids`；
- `company_match_status`；
- `change_type`；
- `quality_status`；
- `translation_status`；
- `dedup_status`；
- `publish_status`；
- `data_version`；
- `content_version`。

公司匹配不明确、质量未通过、翻译落后、完全重复或版本不一致时，内容进入阻断列表并生成反馈，不得推荐旧内容或自行猜测。

## 10. Memory、日志和数据库

Memory 优先保留用户目标、确认事实、关键决定、纠错、来源、未完成任务和近期有效问题；优先丢弃寒暄、重复内容和被新决定覆盖的草稿。

原始会话默认按 15 天规划清理，但必须先 dry-run；审计、来源、人工审核和 legal hold 记录不随普通会话一起删除。

当前 SQLite 用于本地来源记录和幂等验证，不代表正式生产数据库。获得真实数据库权限后应更换存储适配层，不重写上层运营判断。

## 11. 验收命令

完整测试：

```powershell
& $py -m unittest discover -s agent_poc/tests -v
```

旧行为闭环：

```powershell
& $py agent_poc/run_demo.py
```

任意真实问题：

```powershell
& $py agent_poc/run_qwen_smoke.py "你想问的任何企业问题" --limit 5
```

带 Nasdaq 模拟行为上下文的任意问题：

```powershell
& $py agent_poc/run_live_agent.py "你想问的任何企业问题" --limit 5
```

## 12. 8.10 验收标准

1. 旧的 8.06 文件保持不变；
2. 新 Workflow 独立成文并注明上一版本；
3. 命令行问题可自由替换；
4. 问题原文进入模型上下文；
5. 系统根据问题自动选择数据类别；
6. 回答独立展示官网来源；
7. 没有来源时不让模型自由补写事实；
8. 真实提问不冒充已确认长期画像；
9. 原有行为分析和数据交接测试继续通过；
10. 输出继续要求人工审核。

## 13. 下一版本建议

下一版本优先做三件事：

1. 把程少楷的多轮真实提问保存为“待确认行为”，经过重复证据后再形成画像候选；
2. 将行为强度分升级为包含客户匹配、上市需求、时效和互动深度的正式线索评分；
3. 基于真实来源和客户画像生成邮件草稿，但仍由人工审核，不自动发送。

以上三项完成后，运营 Agent 才会从“能判断和回答”继续进化到“知道跟进谁、为什么跟进、应该写什么”。
